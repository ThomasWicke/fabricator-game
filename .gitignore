node_modules/
dist/
.partykit/
.wrangler/
.env
.env.local
.dev.vars
.DS_Store
# eval-art output: regenerable with an API key, ~400KB per image. The three
# curated style refs are embedded in shared/fabricator/style-refs.ts; the
# compiler fixtures ARE committed — they are the free regression suite.
fixtures/art/
fixtures/art-local/

# Backups of .env: these hold live API keys and one reached a commit once.
.env.bak*
.env.*.bak

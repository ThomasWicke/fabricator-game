{
  "name": "fabricator",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently --names client,worker --prefix-colors cyan,magenta \"npm:dev:client\" \"npm:dev:worker\"",
    "dev:client": "vite",
    "dev:worker": "wrangler dev",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.worker.json --noEmit && tsc -p tsconfig.scripts.json --noEmit",
    "test": "npm run typecheck && tsx scripts/test-cost.ts && tsx scripts/test-worldgen.ts && tsx scripts/test-enemies.ts && tsx scripts/test-touchpad.ts && tsx scripts/test-belt.ts && tsx scripts/test-compiler.ts && tsx scripts/test-chroma.ts && tsx scripts/test-chunks.ts && tsx scripts/eval-compiler.ts && tsx scripts/audit-assets.ts && tsx scripts/bench-spawn.ts",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "partyserver": "^0.5.10",
    "partysocket": "^1.1.18",
    "phaser": "^3.90.0",
    "qrcode": "^1.5.4",
    "uuid": "^11.0.3"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.5",
    "@types/uuid": "^10.0.0",
    "concurrently": "^9.2.1",
    "jpeg-js": "^0.4.4",
    "tsx": "^4.23.12",
    "typescript": "^5.7.3",
    "vite": "^6.0.0",
    "wrangler": "^4.125.0"
  }
}
